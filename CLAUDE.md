# CLAUDE.md — KidsCollab Submission

## Version auto-bump
Whenever you make any changes to the project files, increment the version number
in **both** `index.html` (the `<span class="version">` tag) and `index.html` is the
authoritative source for the version. Use semver patch bumps (e.g., 3.3.1 → 3.3.2)
for small changes, and ask before incrementing minor/major.

The current version is **3.3.2**.
